# Aufgabe 8

Diese Aufgabe ist in zwei Teile unterteilt. Lese zuerst nur Teil eins und löse die Aufgabe. Lese erst danach Teil 2.

## Teil 1

Für diesen Teil versuch einmal diese Aufgabe zu lösen mit dem Wissen das du bisher hasst.

Danach fahre fort mit Teil 2.

## Teil 2

Wie du in Teil 1 vielleicht gemerkt hast, ist diese Aufgabe gut lösbar mit dem Wissen, das du bisher hast.
Nun wird noch ein weiteres Element vom Programmieren gezeigt, dass bei dieser Aufgabe nicht notwendig ist, aber bei der
nächsten sehr nützlich werden kann.

### if-else Bedingung

Mit `if` und `else` lässt sich unterschiedlichen code ausführen, anhand einer Bedingung.

```python
if bedingung:
    # code der Ausgeführt wird wen die Bedingung zutrifft
else:
    # code der Ausgeführt wird wen die Bedingung nicht zugetroffen ist
```

### Aufgabe

Versuche nun die Aufgabe nochmals zu lösen und beachte dabei folgende regeln:

1. Verwende ein `if` und `else`.
2. mAche maximal ein Schritt nach unten pro loop